# e.g. `wkg config --edit`
# ```toml
# [namespace_registries]
# gcloud = { registry = "ghcr.io", metadata = { preferredProtocol = "oci", "oci" = { registry = "ghcr.io", namespacePrefix = "a-skua/" } } }
# ```
